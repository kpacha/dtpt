{% extends "layout.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <h2>{{ title }}</h2>
    <ul>
        {% for member in assistency %}
            <li><a href="{{ member._id }}">{{ member._id }}</a> - {{ member.total }}</li>
        {% endfor %}
    </ul>
{% endblock %}